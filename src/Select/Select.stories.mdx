import { Select } from "./Select";
import { Story, Meta } from "@storybook/addon-docs/blocks";

<Meta title="MDX|Select" component={Select} />

# Select

<Story name="Select">
  <div class="component-box">
    <Select
      options={[
        { value: "1", label: "x" },
        { value: "2", label: "y" },
        { value: "3", label: "z" }
      ]}
    />
  </div>
</Story>

<div class="props-table">

## Props

| Prop Name   | Type                                                                                           | Default | Required | Description                             |
| :---------- | :--------------------------------------------------------------------------------------------- | :-----: | :------: | :-------------------------------------- |
| options     | <code>Array<{label: string, value?: string, group?:Array<value:string, label:string> }></code> |    -    |   yes    | Array of options for the select         |
| placeholder | string                                                                                         |    -    |    -     | Placeholder for the select              |
| onChange    | function                                                                                       |    -    |    -     | Callback for when an select has changed |

</div>
